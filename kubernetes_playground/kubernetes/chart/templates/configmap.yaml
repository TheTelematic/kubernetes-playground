apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}
data:
  VERSION: "{{ .Values.image.tag }}"
  {{- range $key, $value := .Values.config }}
  {{ $key }}: "{{ $value }}"
  {{- end }}
